<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="material_mapper">
    <insert id="createMaterial">
        INSERT INTO materials(name)
        VALUES(#{name})
    </insert>

    <resultMap id="material_map" type="race.route.RouteSection" autoMapping="false"> <!-- тег ResultMap позволяет настраивать произвольный мапинг. Делать связи one-to-one и one-to-many
    ResultMap представляет из себя описание правил связи полей EntityBean с колонками из таблиц.-->
        <result property="material" column="name"/>
    </resultMap>
</mapper>